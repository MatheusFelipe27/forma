/**
 * Hover de item clicável. Usa `brand-50` em vez de `neutral-50`: o fundo da
 * página é `neutral-50`, então um hover cinza clareia o item até a cor do
 * fundo e ele se funde com a página em vez de se destacar.
 */
export const CLICKABLE_ROW =
  'transition-colors hover:bg-brand-50 focus-visible:bg-brand-50'
